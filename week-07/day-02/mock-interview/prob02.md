# Problem 2: Find the Difference — Week 7, Day 2 (Mock Interview)

**Difficulty:** Easy

### Description

You are given two strings, `s` and `t`. String `t` is generated by randomly shuffling string `s` and then adding one more letter at a random position.

Return the letter that was added to `t`.

### Function Signature

```python
def prob02(s: str, t: str) -> str:
    pass
```

### Examples

**Example 1:**
```
Input:  s = "abcd", t = "abcde"
Output: "e"
```

**Example 2:**
```
Input:  s = "", t = "y"
Output: "y"
```

### Constraints

- `0 <= s.length <= 1000`
- `t.length == s.length + 1`
- `s` and `t` consist of lowercase English letters.
